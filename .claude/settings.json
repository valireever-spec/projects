{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Read",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q 'reviews/' && echo '{\"systemMessage\": \"📋 Quick reminder: Use `bash scripts/prepare-review.sh /path/to/project ProjectName` to auto-prep your review\"}' || true"
          }
        ]
      }
    ]
  }
}
